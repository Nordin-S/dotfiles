require("copilot").setup({
	filetypes = {
		markdown = true, -- overrides default
		typescript = true, -- overrides default
		typescriptreact = true, -- overrides default
		javascript = true, -- overrides default
		javascriptreact = true, -- overrides default
		rust = true, -- overrides default
		go = true, -- overrides default
		python = true, -- overrides default
		cpp = true, -- overrides default
		c = true, -- overrides default
		lua = true, -- overrides default
		vim = true, -- overrides default
		sh = true, -- overrides default
		zsh = true, -- overrides default
		yaml = true, -- overrides default
		yml = true, -- overrides default
		json = true, -- overrides default
	},
})
